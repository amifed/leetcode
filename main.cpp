#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;
#define LOCAL
int main(int argc, char const *argv[]) {
#ifdef LOCAL
    freopen("data.in", "r", stdin);
    freopen("data.out", "w", stdout);
#endif
    int a, b;
    while (~scanf("%d %d", &a, &b)) {
        printf("%d %d\n", a, b);
    }
    return 0;
}
