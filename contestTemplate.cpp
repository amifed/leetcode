#include <bits/stdc++.h>
using namespace std;
#define all(v) (v).begin(), (v).end()
using LL = long long;
using PII = pair<int, int>;
using PIL = pair<int, LL>;
using PLL = pair<LL, LL>;
const int MOD = 1e9 + 7;
const int INF = 1e9 + 7;

class Solution {
public:
};

int main() {
    Solution solution;
    int a, b, c;
    cin >> a >> b >> c;
    string s;
    cin >> s;
    cout << (a + b + c) << ' ' << s << endl;
    return 0;
}